\documentclass[11pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{xcolor}
\usepackage[hidelinks]{hyperref}

\setlength{\emergencystretch}{3em}

\definecolor{DoneGreen}{RGB}{20,115,70}
\definecolor{RunOrange}{RGB}{180,90,0}
\definecolor{PendRed}{RGB}{160,25,25}
\newcommand{\done}{\textcolor{DoneGreen}{\textbf{$\checkmark$ Done}}}
\newcommand{\running}{\textcolor{RunOrange}{\textbf{Running}}}
\newcommand{\pending}{\textcolor{PendRed}{\textbf{Pending}}}
\newcommand{\deferred}{\textcolor{RunOrange}{Deferred}}
\newcommand{\blocked}{\textcolor{PendRed}{\textbf{Blocked}}}
\newcommand{\skipped}{Skipped (optional)}
\newcommand{\partialstatus}{\textcolor{RunOrange}{\textbf{Partial}}}
\newcommand{\ready}{\textcolor{RunOrange}{\textbf{Code ready}}}

\title{\textbf{Experiment Progress and Next-Step Plan}}
\author{}
\date{9 September 2026, after the overnight GPU session --- IEEE TIFS, fifth
independent review cycle. This file carries \emph{only} open, running and
not-yet-started work; completed items are removed once recorded in
\texttt{docs/progress.md}.}

\begin{document}
\maketitle

\section*{Scope of This Revision Cycle}

The active review is the fifth independent review in
\texttt{docs/RevisionSuggestions.tex} (8 September 2026, IEEE TIFS, verdict
\emph{not ready for submission; major scientific revision}). Its thirteen
findings split into text-and-analysis items, which this cycle has largely
closed (R1, R2, R3, R4, R12, R13 --- Table~3), and items that need experiments
(R6, R7, R8, R9, R10) or a packaging pass (R11). The review's recommended
experiments E1--E8 are planned in \texttt{docs/Design.md} as A1--A8; the
renaming avoids a collision with the older TOMM E1--E7 in the same file.

\paragraph{Document state, rebuilt and verified on 9 September 2026.}
\texttt{main.pdf} 13 pages (the TIFS initial-submission ceiling, with no spare
page), abstract 246 words, three figures; \texttt{supplementary.pdf} 4 pages;
\texttt{backup/supplementary\_extended.pdf} 12 pages, not submitted, shipped
with the artifact. Both submitted documents: 0 undefined references, 0
undefined citations. 30 unit tests pass; \texttt{verify\_claims.py} reports
228 claims and 0 mismatches.

\section*{Table 1: Open and Partial Items}

\begin{longtable}{@{}p{0.05\linewidth}p{0.27\linewidth}p{0.12\linewidth}p{0.48\linewidth}@{}}
\caption{Everything still open after the fifth TIFS review cycle.}
\label{tab:tifs5-open}\\
\toprule
ID & Item & Status & Completion gate / next step \\
\midrule
\endfirsthead
\toprule
ID & Item & Status & Completion gate / next step \\
\midrule
\endhead
\midrule
\multicolumn{4}{r}{\itshape continued on next page} \\
\endfoot
\bottomrule
\endlastfoot

R1 & Prose, generated tables and exports described different numerical
versions. & \partialstatus & Every headline number in the main text now reads
from \texttt{paper/generated/tab\_transfer.tex} and the verified utility
export (Table~3). \textbf{Still open}: the D6 per-query rows behind that table
are on no local drive, so Top-5/10 and place-clustered re-analysis cannot be
computed, and \texttt{audit\_claim\_consistency.py} (planned, A1) is not
written yet. Gate: recover or regenerate the D6 export, then one script that
fails on any prose-versus-table mismatch. \\

R2 & The allocation-cannot-decide-ranking claim was asserted from an identity
about expected squared displacement. & \done & Replaced by the margin-variance
analysis with an explicit two-coordinate counterexample; section retitled; the
abstract, introduction, contributions and conclusion no longer state an
impossibility. \\

R3 & ``Oracle'', ``strictly correct'' and ``bound'' exceed what is measured.
& \partialstatus & The optimality claim is disclaimed and the rule is
described as a renormalised approximation to a corner solution. \textbf{Still
open}: the statistic is still called a gradient concentration where the paper
means Jacobian column norms. Gate: experiment A2 measures both and the text
names whichever was measured. \\

R4 & Directly relevant prior work was missing. &
\textcolor{DoneGreen}{\textbf{Measured}} & Le et al.\ and GeoShield are cited,
and A4 ran the mask-guided baseline under this protocol at three coverages
and three mask sources. The mask never beats the full-frame direction and
loses monotonically as it tightens ($+0.063$ at coverage $0.10$, $+0.002$ at
$0.50$), independently of which mask is used. \textbf{Remaining}: write the
comparison into the paper, which turns the novelty claim into a tested one. \\

R5 & Inference unit, clustering, multiplicity, equivalence margins. &
\partialstatus & The transfer table already uses the query as the unit, and
this cycle removed the stale pair-level $p$-values from the prose.
\textbf{Still open}: place-level clustering (277 places, up to 12 queries
sharing one), a stated multiplicity policy, and equivalence margins for every
``no benefit'' sentence. Needs the D6 rows; part of A1. \\

R6 & The protocol does not isolate magnitude, sign and placement. &
\textcolor{DoneGreen}{\textbf{Measured}} & Experiment A3 ran at both budgets
on both attackers: shuffling the signs removes the whole effect
($p=0.33$ and $0.66$ at the operating point), flattening the magnitudes keeps
$49$--$99\%$ of it, and pushing an optimised direction through edge placement
destroys most of it. \textbf{Remaining}: write it into the manuscript, which
replaces a qualitative claim with a decomposition. \\

R7 & Matched distortion is not a utility frontier. & \pending & Experiment A5:
sweep four measured budgets and report privacy against detector/segmenter
metrics with a declared utility tolerance. Not started; needs a GPU session. \\

R8 & The adaptation claim rests on a restricted attacker. & \partialstatus &
The claim is now scoped in the text to the budget that produced it.
\textbf{Still open}: experiment A6 --- adapt against the hardened release,
rebuild the gallery, evaluate on a place-disjoint split. \\

R9 & Benchmark and attacker generalisation. & \partialstatus & The benchmark
is described as a custom subset and the cluster-label criterion is
distinguished from the dataset's distance-and-heading protocol. A7 replicated
the direction contrast on both official cross-time subtasks: four of four
cells significant ($-0.038$ to $-0.176$). \textbf{Still open}: a second
dataset, one more held-out VPR architecture, and the GPS/heading audit of
cluster positives. \\

R10 & Top-1 is not location privacy; the release boundary is incomplete. &
\partialstatus & Top-5/10 are now measured for every new condition, and they
change the reading: against MixVPR the direction moves Top-1 $0.785\to0.734$
while Top-5 only moves $0.870\to0.830$. The release boundary is measured too
--- maximum perturbation reaches $74$--$76$ against an $\ell_\infty$
projection of $16$, because the rescaling follows the projection.
\textbf{Still open}: reporting both in the manuscript, and A8 on the decoded
file. \\

R11 & The artifact is not a complete, current reviewer package. &
\partialstatus & The builder was repaired and now assembles all seventeen
export trees, ships the extended report, and verifies 228 claims from a clean
rebuild. \textbf{Still open}: it does not cover the D6 exports or the
generated-table summaries, and the paper's code URL has not been confirmed
publicly reachable. \\

R12 & Method and optimiser need a complete specification. & \partialstatus &
The optimiser's steps, projection, random start and BPDA substitution are
stated. \textbf{Still open}: explicit definitions for the low-pass,
correlated-noise and block-quantisation operators and for the EOT objective. \\

R13 & Interpretation and display errors. & \done & Sign error, ratio,
comparison scope, seed sets, per-image versus dataset-level metrics, and the
process-history sentences are all corrected (Table~3). \\

V1 & \texttt{USAGE.md} is gitignored, so its venue line is unprotected. &
\pending & Unchanged from the previous cycle; awaiting the user's choice. \\

\end{longtable}

\section*{Table 2: Next-Step Plan}

\begin{longtable}{@{}p{0.19\linewidth}p{0.09\linewidth}p{0.05\linewidth}p{0.18\linewidth}p{0.41\linewidth}@{}}
\caption{Scheduled work as of 9 September 2026, 02:00 NZST. Nothing is
running: the overnight session finished at 01:38 and both billed hosts were
powered down and confirmed unreachable. Times are New Zealand Standard Time.}
\label{tab:tifs5-schedule}\\
\toprule
Item & Status & \% & Est.\ completion (NZST) & Purpose and notes \\
\midrule
\endfirsthead
\toprule
Item & Status & \% & Est.\ completion (NZST) & Purpose and notes \\
\midrule
\endhead
\midrule
\multicolumn{5}{r}{\itshape continued on next page} \\
\endfoot
\bottomrule
\endlastfoot

A3 --- magnitude/sign/placement factorial (R6, R10) & \done & 100\%
& Finished 01:38 NZST, 9 September & 38,400 rows over two budgets and two
attackers, all six jobs per cell exiting zero. Reproduction gate passed:
MixVPR isotropic $0.7850$ against a published $0.7800$ and direction $0.7342$
against $0.7317$; ResNet18 within $0.008$. Results in
\texttt{docs/progress.md} entries 318--320. \\

A4 --- mask-guided PGD baseline (R4) & \done & 100\% & Finished 01:36 NZST,
9 September & 28,800 rows: the CAM analogue at three coverages on both
attackers, plus edge and saliency masks at $0.25$. The mask never wins.
Entry 321. \\

A7 --- cross-time external validity (R9) & \done & 100\% & Finished 01:32
NZST, 9 September & 9,600 rows on both official cross-time subtasks, four of
four cells significant. Entry 322. \\

\textbf{Paper writeback} --- A3, A4, A7 into the manuscript & \pending & 0\%
& Next working session, no GPU & \textbf{The critical path now.} The
decomposition replaces the qualitative direction-versus-allocation claim in
\S\,What Allocation Moves; the mask baseline replaces the argument in
\S\,Why we do not re-run a published system; Top-1/5/10 must be reported
together per R10. All three additions land in a manuscript that has no spare
page, so the writeback has to be planned against the page budget. \\

A1 --- freeze one export per family, reconcile every number (R1, R5, R10) &
\partialstatus & 40\% & After the writeback & The D6 rows are still missing,
but A3's regenerated \texttt{direction}/\texttt{isotropic} cells now provide a
frozen, artifact-covered export for that family. Remaining: place-clustered
bootstrap, equivalence margins, and the prose-versus-table audit script. \\

A2 --- what the sensitivity maps measure (R2, R3) & \pending & 10\% (design
only) & Local GPU, 2\,h, any time & Random-projection estimate of Jacobian
column norms against the score-gradient and margin-gradient maps. Small
enough for the RTX 3070; scheduled after A3 launches so it does not compete
for attention. \\

A8 --- what is actually released (R10) & \pending & 10\% (design only) &
Local GPU, 2\,h, any time & Save, decode and re-measure the released frames as
PNG and JPEG. Checks whether the post-optimisation rescaling can exceed the
$\ell_\infty$ projection that preceded it. \\

A5 --- privacy against measured utility over budgets (R7) & \pending & 5\%
& Second GPU session & Four measured budgets, four conditions including the
hardened direction, with a declared utility tolerance. \\

A6 --- adaptation against the hardened release (R8) & \pending & 5\% &
Second GPU session & Fine-tune on hardened releases, rebuild the gallery,
evaluate on a place-disjoint split. \\

A7 --- external validity (R9) & \pending & 5\% & Second GPU session &
City-disjoint holdout of the existing 8-city manifest plus one more held-out
strong VPR backbone; no new download needed. \\

R11 --- artifact coverage & \partialstatus & 70\% & After A3 & Add the D6 and
factorial exports and the generated-table summaries to the bundle; confirm the
published code URL resolves. \\

\end{longtable}

\section*{Table 3: Manuscript Edits}

\begin{longtable}{@{}p{0.05\linewidth}p{0.30\linewidth}p{0.10\linewidth}p{0.47\linewidth}@{}}
\caption{Text and analysis items from the fifth review, closed on 9 September
2026 and kept visible for one cycle so the next review does not rediscover
them.}
\label{tab:tifs5-text}\\
\toprule
ID & Edit & Status & Location \\
\midrule
\endfirsthead
\toprule
ID & Edit & Status & Location \\
\midrule
\endhead
\midrule
\multicolumn{4}{r}{\itshape continued on next page} \\
\endfoot
\bottomrule
\endlastfoot

T14 (R1) & Ten numerical mismatches between prose and the generated table
corrected: Top-1 $0.0358\to0.0317$ and $0.7342\to0.7317$, effects
$-0.1608\to-0.1650$ and $-0.0458\to-0.0483$, pair-level $p$-values
$2\times10^{-41}$ and $2\times10^{-8}$ replaced by the query-level
$1\times10^{-15}$ and $3\times10^{-6}$, white box $0.0033\to0.0058$ with the
two operating points separated, recovery share $83\to86\%$, and the abstract
and conclusion resynchronised. & \done & \texttt{main.tex} abstract,
Contributions, \S\,The Other Axis, Conclusion. \\

T15 (R2) & \S\,``Why Allocation Cannot Decide a Ranking'' retitled and
rewritten around $\Pr\{m<0\}\approx\Phi(-m/\sqrt{v(w)})$ with
$v(w)=\sigma^2\sum a_i^2w_i^2$, an explicit counterexample, and the null
restated as empirical. Universal statements removed from the abstract,
introduction and contributions. & \done & \texttt{main.tex}
\S\ref{tab:tifs5-text} region: Introduction, Contributions,
\S\,What Allocation Moves. \\

T16 (R4) & \texttt{le2022rethinking} and \texttt{liu2026geoshield} added and
cited; the novelty claim repositioned to the matched-distortion comparison;
the appeal to an unnamed 2024 survey removed. & \done & \texttt{main.tex}
\S\,Null on Real Data; \texttt{ref.bib}. \\

T17 (R3) & The optimality reading of the gradient-guided placement disclaimed
where the identity is introduced. & \done & \texttt{main.tex}
\S\,What Allocation Moves. \\

T18 (R13) & Delivered-MSE ratio $8\times\to15.4\times$; allocation floor
quoted for the best operator as well as additive noise; the segmentation
utility delta corrected to the export's $-0.048$ at $p=5\times10^{-9}$ and
labelled per-image; seed sets named per experiment family; process-history
sentences removed. & \done & \texttt{main.tex} \S\,Operators,
\S\,The Other Axis, \S\,Experimental Protocol. \\

T19 (R5, R8) & ``Inert'', ``does not recover Top-1'' and ``wins outright''
replaced by scoped statements; the adaptation result bounded by the budget
that produced it; the unverifiable ``23 further paired tests'' count removed.
& \done & \texttt{main.tex} abstract, \S\,Scope of the claims,
\S\,Adaptive. \\

T20 & Body compressed to hold 13 pages and the abstract to 246 words after
the additions above. & \done & \texttt{main.tex} throughout. \\

\end{longtable}

\section*{Closed in Previous Cycles}

The fourth review's items (R1--R10 of 8 September, D6/D7) are recorded in
\texttt{docs/progress.md} entries 274--298, together with the recovery of
\texttt{icme2027\_placement\_msls} and the closure of R1 on the placement and
operator tables. D1--D5, the third review, ICME 2027 (M1--M9), TOMM (E1--E7)
and the 3 September items remain closed.

\end{document}
