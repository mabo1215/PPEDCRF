\documentclass[11pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{xcolor}
\usepackage[hidelinks]{hyperref}

\setlength{\emergencystretch}{3em}

\definecolor{DoneGreen}{RGB}{20,115,70}
\definecolor{RunOrange}{RGB}{180,90,0}
\definecolor{PendRed}{RGB}{160,25,25}
\newcommand{\done}{\textcolor{DoneGreen}{\textbf{$\checkmark$ Done}}}
\newcommand{\running}{\textcolor{RunOrange}{\textbf{Running}}}
\newcommand{\pending}{\textcolor{PendRed}{\textbf{Pending}}}
\newcommand{\blocked}{\textcolor{PendRed}{\textbf{Blocked}}}
\newcommand{\partialstatus}{\textcolor{RunOrange}{\textbf{Partial}}}

\title{\textbf{Experiment Progress and Next-Step Plan}}
\author{}
\date{9 September 2026, 16:10 NZST --- IEEE TIFS, fifth independent review
cycle, after the A2/A5/A6/A7b/A8 experiment round and the first manuscript
writeback. This file carries \emph{only} open, running and not-yet-started
work; completed items are removed once recorded in \texttt{docs/progress.md}.}

\begin{document}
\maketitle

\section*{Where This Cycle Stands}

The active review is the fifth independent review in
\texttt{docs/RevisionSuggestions.tex} (8 September 2026, IEEE TIFS, verdict
\emph{not ready for submission; major scientific revision}), findings R1--R13.
All experiments it called for have now been run. \textbf{Nine of the thirteen
findings are closed}; the four that remain are blocked on export trees that are
not on any reachable machine, on a page budget, or on the authors.

\paragraph{Compute state, 9 September 2026, 16:10 NZST.} \textbf{All four hosts
are shut down} --- PRO 6000, 2c, vGPU 3090 and H800 all refuse the connection.
Nothing is running or queued anywhere, and nothing needs to be: no open item
below requires a GPU except the conditional regeneration in X1.

\paragraph{Document state.} \texttt{main.pdf} \textbf{15 pages} against the
13-page TIFS initial-submission ceiling --- content was added first by
instruction and the compression pass is the next task. Abstract 250 words
(window 150--250, no margin). \texttt{supplementary.pdf} 5 pages. Both build
with 0 undefined references, 0 undefined citations, 0 overfull boxes.
\textbf{140} claims verify against raw exports (was 110), \textbf{52} unit
tests pass.

\paragraph{One provenance gap opened by shutting 2c down.} The A6
\emph{training} logs and checkpoints (\texttt{src/outputs/tifs\_a6}) were never
copied off 2c; only the evaluation exports were. The manuscript quotes two
numbers from those logs --- validation MRR $0.047$ and $0.058$ at the selected
epoch~8 --- which therefore have no local provenance and are not in the
auditor. Either 2c is restarted for a two-minute copy, or those two figures
come out of the text. Everything else in \S\,``An Attacker That Adapts'' rests
on \texttt{tifs\_a6\_eval}, which is local.

\section*{Table 1: Open Items Against the Fifth Review}

\begin{longtable}{@{}p{0.05\linewidth}p{0.24\linewidth}p{0.11\linewidth}p{0.52\linewidth}@{}}
\caption{The four findings still open, and why. R2, R3, R4, R6, R7, R8, R9,
R10, R12 and R13 are closed and no longer listed.}
\label{tab:tifs5-open}\\
\toprule
ID & Item & Status & What remains \\
\midrule
\endfirsthead
\toprule
ID & Item & Status & What remains \\
\midrule
\endhead
\midrule
\multicolumn{4}{r}{\itshape continued on next page} \\
\endfoot
\bottomrule
\endlastfoot

R1 & Prose, tables and exports described different numerical versions. &
\partialstatus & \textbf{Direction family closed}: 140 of 140 claims verify
against raw per-query rows, including the 30 added for A2, A5 and A8. The
auditor caught two of my own rounding errors while being extended. \textbf{Two
gaps remain}: (i) the allocation family's twelve numbers rest on
\texttt{icme2027\_placement\_msls} and \texttt{operator\_study}, still missing;
(ii) the A6 training-side MRR figures have no local provenance (see above). \\

R5 & Inference unit, clustering, multiplicity, equivalence margins. &
\partialstatus & Closed for the direction family --- place-clustered bootstrap
over 277 places, a named primary family under Holm, a $\pm0.01$ equivalence
margin fixed in advance. \textbf{Still open}: the same treatment for the
allocation family, which needs the same two missing trees as R1. \\

R11 & The artifact is not a complete, current reviewer package. & \blocked &
Eight trees still missing: \texttt{icme2027\_placement\_msls},
\texttt{tifs5\_analysis}, \texttt{margin\_oracle}, \texttt{operator\_study},
\texttt{known\_jacobian}, \texttt{known\_jacobian\_operators},
\texttt{d4\_3seed}, \texttt{sanfree\_3seed}. \texttt{build\_artifact.sh} exits
1 on the first absent tree. Recovery is exhausted --- all four hosts and every
mounted local drive searched. Gate: X1. \\

Pkg & Page budget and submission mechanics. & \pending & \textbf{15 pages
against a 13-page ceiling.} Abstract at 250 words has no margin either. Cover
letter is rewritten; ORCIDs, EDICS and submission-system metadata are the
authors' to complete. \\

V1 & \texttt{USAGE.md} is gitignored, so its venue line is unprotected. &
\pending & Awaiting the user's decision. \\

\end{longtable}

\section*{Table 2: Next-Step Plan}

Only unfinished work appears below; every completed experiment has been removed
and is recorded in \texttt{docs/progress.md}. Times are New Zealand Standard
Time. Nothing here needs a GPU except X1's conditional branch.

\begin{longtable}{@{}p{0.19\linewidth}p{0.07\linewidth}p{0.05\linewidth}p{0.17\linewidth}p{0.44\linewidth}@{}}
\caption{Open work as of 9 September 2026, 16:10 NZST. No host is running.}
\label{tab:tifs5-schedule}\\
\toprule
Item & Status & \% & Est.\ completion (NZST) & Purpose and notes \\
\midrule
\endfirsthead
\toprule
Item & Status & \% & Est.\ completion (NZST) & Purpose and notes \\
\midrule
\endhead
\midrule
\multicolumn{5}{r}{\itshape continued on next page} \\
\endfoot
\bottomrule
\endlastfoot

\textbf{P1 --- compress 15 pages to 13} (Pkg) & \pending & 0\% &
\textbf{10 Sep, $\approx$12:00} (3\,h, no GPU) & \textbf{The critical path to
submission.} Candidates, in the order I would cut them: \S\,Secondary
Benchmarks entirely ($\approx$0.6\,page; it evaluates the additive mechanism,
not the final release, and says so); the overlap between
\S\,Robustness to Non-Adaptive Preprocessing and the new \S\,What Is Actually
Released ($\approx$0.4\,page); the superseded learning-rate narrative in
\S\,An Attacker That Adapts ($\approx$0.3\,page); the budget-sweep prose that
Table~III already carries ($\approx$0.3\,page). Needs the user's agreement on
what leaves the main text. \\

\textbf{P2 --- recover A6 training provenance} (R1) & \pending & 0\% &
\textbf{2c up $+$ 5\,min} & Copy \texttt{src/outputs/tifs\_a6} and
\texttt{logs/a6.log} off 2c, then register the two MRR figures in the auditor.
\emph{Or} delete those two figures from the manuscript, which costs nothing
scientifically --- the argument rests on the evaluation numbers, which are
local. Decide before the compression pass so the text is only edited once. \\

\textbf{P3 --- second writeback pass} (R1, R13) & \pending & 0\% &
\textbf{10 Sep, $\approx$15:00} (2\,h, no GPU) & Fold the A6/A7b numbers now in
\texttt{progress.md} into the auditor as claims, re-read the whole manuscript
for sentences the new evidence contradicts, and re-check every figure caption
against its generator the way Figure~3 was just checked. Figure~3's drift was
found only because I went looking; the other figures have not had the same
audit. \\

X1 --- the eight missing export trees (R1, R5, R11) & \blocked & 33\% &
\textbf{Awaiting the home-machine check} & Four of twelve recovered; recovery
otherwise exhausted. \texttt{docs/missing\_exports\_hunt.md} carries the search
spec. If they are gone, regeneration on 2c costs $\approx$6\,h and produces
\emph{new} numbers, so \S\,``The Null Holds on Real Geographic Data'' and the
operator table would need rewriting --- not to be started without the user's
go-ahead. \\

R11 --- artifact rebuild and verification & \blocked & 0\% & X1 $+$ 2\,h &
Rebuild from a clean extraction, add the claim auditor to
\texttt{run\_verification.sh}, confirm the published code URL resolves
publicly. Cannot start before X1. \\

A7b-angular --- the heading criterion (R9) & \pending & 0\% &
\textbf{Conditional} & The distance half is done (99.3\% agreement at 25\,m).
The angular half needs the raw MSLS \texttt{postprocessed.csv} heading column,
which is not in the staged subset and is not on any reachable host. Either
stage it from the original download or leave the limitation stated in the text,
which it already is. \\

V1 --- protect the venue line & \pending & 0\% & \textbf{Awaiting decision} &
\texttt{USAGE.md} is gitignored. Either commit a tracked copy of the venue
declaration or accept that it lives outside version control. \\

\end{longtable}

\section*{Table 3: Manuscript Edits This Session}

\begin{longtable}{@{}p{0.06\linewidth}p{0.30\linewidth}p{0.09\linewidth}p{0.47\linewidth}@{}}
\caption{Writeback of the experiment round, and the defects the cross-check
turned up. Kept visible for one cycle.}
\label{tab:tifs5-text}\\
\toprule
ID & Edit & Status & Location \\
\midrule
\endfirsthead
\toprule
ID & Edit & Status & Location \\
\midrule
\endhead
\midrule
\multicolumn{4}{r}{\itshape continued on next page} \\
\endfoot
\bottomrule
\endlastfoot

T34 (R8) & \S\,An Attacker That Adapts rewritten. Both attacker models now
reported; the claim that a fixed gallery index is a neutral restriction is
retired, because the fixed-index attacker \emph{loses} $0.135$--$0.180$ against
baseline, every interval excluding zero. Selection moved to MRR, with the
reason stated: under the directional exposures Top-1 is zero at every epoch, so
Top-1 selection returns the pretrained model by construction. & \done &
\texttt{main.tex}, \S\,An Attacker That Adapts. \\

T35 (R7) & The sentence ``We do not have that frontier'' replaced by the
frontier, over four budgets, with both readings: direction dominates at matched
utility, \emph{and} at a $0.05$ mIoU tolerance nothing on offer meaningfully
degrades the attacker. New generated table \texttt{tab\_frontier}. & \done &
\texttt{main.tex} \S\,The Other Axis; \texttt{generated/tab\_frontier.tex}. \\

T36 (R10) & New subsection \S\,What Is Actually Released: the amplitude bound
is a property of the budget, not the mechanism; matched distortion on tensors
is not matched distortion on files. & \done & \texttt{main.tex}. \\

T37 (R9) & Third held-out backbone (Patch-NetVLAD, $-0.1917$
$[-0.2320,-0.1507]$) and the cluster-versus-geographic positives audit
($99.3\%$ at 25\,m, median $5.6$\,m, angular half declared unreproducible). &
\done & \texttt{main.tex} \S\,The Other Axis, \S\,Experimental Protocol. \\

T38 (R2, R3) & Eq.~(displacement) is stated in $\lVert J_i\rVert$, so the
column norms are now measured rather than approximated by the score gradient:
concentrations $0.477$ and $0.653$, agreement $0.656$ against a split-half
ceiling of $0.982$. R2's counterexample becomes a measurement: allocation moves
the margin-flip rate from $5.0\%$ to $9.2\%$ at fixed energy. & \done &
\texttt{main.tex}, \S\,What Allocation Moves. \\

D1 (R1, R13) & \textbf{Figure~3 was drawn from stale constants.} Its docstring
claimed transcription from \texttt{tab\_sanitize.tex} ``so the figure and the
table cannot disagree''; they had drifted by up to $0.014$, and the figure
marked MixVPR/denoise non-significant although its interval
$[-0.0367,-0.0017]$ excludes zero. \texttt{fig\_sanitize} now parses the table,
so the invariant holds by construction. & \done &
\texttt{make\_tifs\_review\_figures.py}. \\

D2 (R13) & \texttt{tab\_sanitize}'s caption said ``the first five are the
transforms the hardening was optimised over''. Four were; the fifth row is the
identity. The main-text figure caption had been fixed and this one had not. &
\done & \texttt{generated/tab\_sanitize.tex}. \\

D3 (R13) & R13's fourth item was not closed: the controlled-model passage
called operators interchangeable and concluded spatial structure was worth as
little in the operator as in the map, while reporting placement gains of
$-0.42$ to $-0.48$ in that same model. The two axes are now separated
explicitly. & \done & \texttt{main.tex} \S\,Allocation II. \\

D4 & ``Quality-preserving'' survived in four places after the frontier section
retired the term, leaving the paper contradicting itself. Now
``low-distortion'' except where the retraction itself is stated. & \done &
\texttt{main.tex}. \\

C14 & Auditor extended from 110 to 140 claims (A2, A5, A8 families). It caught
two rounding errors I had introduced: a gain printed $1.14$ that recomputes to
$1.146$, and $0.619$ that recomputes to $0.618$. & \done &
\texttt{audit\_claim\_consistency.py}. \\

\end{longtable}

\section*{Closed in This Cycle}

R2, R3, R4, R6, R7, R8, R9, R10, R12 and R13 are closed; their evidence and the
experiments behind it are recorded in \texttt{docs/progress.md} entries
343--361. All eight experiments the review asked for (A1--A8) have been run:
A1 and A7b's audit on CPU, A2 and A8 on PRO 6000, A5, A6 and A7b on 2c, with
A3, A4 and A7's cross-time arms carried over from earlier cycles.

\end{document}
