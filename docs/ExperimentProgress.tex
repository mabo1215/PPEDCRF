\documentclass[11pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{xcolor}
\usepackage[hidelinks]{hyperref}

\setlength{\emergencystretch}{3em}

\definecolor{DoneGreen}{RGB}{20,115,70}
\definecolor{RunOrange}{RGB}{180,90,0}
\definecolor{PendRed}{RGB}{160,25,25}
\newcommand{\done}{\textcolor{DoneGreen}{\textbf{$\checkmark$ Done}}}
\newcommand{\running}{\textcolor{RunOrange}{\textbf{Running}}}
\newcommand{\pending}{\textcolor{PendRed}{\textbf{Pending}}}
\newcommand{\deferred}{\textcolor{RunOrange}{Deferred}}
\newcommand{\blocked}{\textcolor{PendRed}{\textbf{Blocked}}}
\newcommand{\skipped}{Skipped (optional)}
\newcommand{\partialstatus}{\textcolor{RunOrange}{\textbf{Partial}}}
\newcommand{\ready}{\textcolor{RunOrange}{\textbf{Code ready}}}

\title{\textbf{Experiment Progress and Next-Step Plan}}
\author{}
\date{8 September 2026 (evening) --- IEEE TIFS, fourth independent review
cycle. This file carries \emph{only} open, running and not-yet-started work;
completed items are removed once recorded in \texttt{docs/progress.md}.}

\begin{document}
\maketitle

\section*{Scope of This Revision Cycle}

The active review is the fourth independent review in
\texttt{docs/RevisionSuggestions.tex} (8 September 2026, IEEE TIFS, verdict
\emph{major revision}). Its ten findings split into text-only items, which are
all closed (R2, R5, R6, R7, R9, R10 --- see Table~3 and
\texttt{docs/progress.md} entries 263--272), and three items that need either
data this machine cannot reach (R1) or a GPU session (R3/D6, R4/D7). Those,
plus the supplement-length decision (R8), are everything below.

\paragraph{Document state, rebuilt and verified on 8 September 2026.}
\texttt{main.pdf} 13 pages with three figures (protocol schematic, budget
dependence, preprocessing and hardening), abstract 246 words, index terms
present; \texttt{supplementary.pdf} 12 pages with its own reference list.
Both: 0 undefined references, 0 undefined citations, 0 overfull boxes, no
missing bibliography entries; 22 rendered-PDF checks pass; 13 unit tests pass.

\section*{Table 1: Open and Partial Items}

\begin{longtable}{@{}p{0.05\linewidth}p{0.27\linewidth}p{0.12\linewidth}p{0.48\linewidth}@{}}
\caption{Everything still open after the fourth TIFS review cycle.}
\label{tab:tifs-open}\\
\toprule
ID & Item & Status & Completion gate / next step \\
\midrule
\endfirsthead
\toprule
ID & Item & Status & Completion gate / next step \\
\midrule
\endhead
\midrule
\multicolumn{4}{r}{\itshape continued on next page} \\
\endfoot
\bottomrule
\endlastfoot

R1 & Every paired test in the direction, preprocessing, EOT, operator and
MSLS-placement tables treats (query, seed) as the unit, so ``$n=1{,}200$'' is
400 queries $\times$ 3 seeds and every $p$-value is overstated. The proxy
placement study and the E1 analysis already use the query/place cluster as
the unit; the rest of the paper does not. & \blocked & Tool ready:
\texttt{analyze\_direction\_transfer.py --unit query} (seed-averaged per
query, query-cluster bootstrap 95\% CI, Wilcoxon), exercised on synthetic
data. Blocked on the per-query exports under
\texttt{src/outputs/icme2027\_*}, which are not on this machine. Gate:
every $\Delta$ in Tables~1--3, Fig.~3 and the supplement carries a
query-level CI; ``paired observations'' replaced by ``400 queries, three
seeds''; the marginal MixVPR cells re-marked; the sentence ``a property of
the objective rather than a shortage of data'' re-earned or removed. No GPU. \\

R3 & The EOT-hardened direction is evaluated only against the four
transforms it was optimised over. & \ready & Experiment D6 (Table~2). Eight
held-out attacker-side transforms and \texttt{--eval\_sanitizers} are
implemented and tested; the run needs one GPU session per backbone. Gate: the
\texttt{none} and four trained cells reproduce the published values to
rounding, and the held-out block is written into the paper with query-level
inference, whichever way it comes out. \\

R4 & The utility cost (per-image AP@50, IoU) is measured for the unhardened
direction only, while the paper now leads with the hardened one. & \ready &
Experiment D7 (Table~2). \texttt{evaluate\_direction\_utility.py
--eot\_sanitizers} adds a \texttt{hardened\_direction} condition sharing the
unhardened direction's random start. Gate: one row in the supplementary
utility table, one sentence in \S\,The Other Axis. Fits the local GPU. \\

R8 & Supplementary Material is 12 pages against the SPS six-page rule, which
needs Editor-in-Chief approval to exceed. & \pending & A decision: request
approval in the cover letter, or split the legacy proxy sections
(sequence-length, matched-PSNR and extensions, proxy50, energy-matched full
data) into a repository technical report and keep the reviewer-facing
material within six pages. The review recommends the split; the
repository's standing decision is to keep the current length. \\

C1 & The code URL \texttt{github.com/mabo1215/PPEDCRF} was restored to the
author footnote per review R7. & \pending & Confirm the repository is public
and holds the evaluation exports the paper describes; otherwise point the
footnote at the artifact bundle instead. \\

F3 & SHA-256 provenance for the paper-facing export trees. & \done &
Closed 8 September 2026: eleven trees pulled from the GPU host and hashed
independently on both machines, all eleven digests matching. Record in the
experiment provenance section of \texttt{docs/progress.md}, which also names
the two gaps left open --- \texttt{icme2027\_placement\_msls} is on no
reachable machine, and the surviving direction exports are a subset of what
the published transfer table reports, which is why D6 regenerates it. \\

B3 & Neither checkpoint of the tested mechanism was engineered as a privacy
map. & \partialstatus & Disclosed rather than closed; no action planned. \\

V1 & \texttt{USAGE.md} is gitignored, so its venue line cannot be protected
by version control and reverted to ICLR 2027 once already. & \pending &
Either un-ignore the file or move the venue line into a tracked file that
the rules read. Awaiting the user's choice. \\

\end{longtable}

\section*{Table 2: Next-Step Plan}

\begin{longtable}{@{}p{0.20\linewidth}p{0.08\linewidth}p{0.06\linewidth}p{0.16\linewidth}p{0.42\linewidth}@{}}
\caption{Scheduled work as of 8 September 2026. Both billed GPU hosts are
powered down; nothing is running. Estimated completion times are New Zealand
Standard Time and start from GPU-on.}
\label{tab:tifs-schedule}\\
\toprule
Item & Status & \% & Est.\ completion (NZST) & Purpose and notes \\
\midrule
\endfirsthead
\toprule
Item & Status & \% & Est.\ completion (NZST) & Purpose and notes \\
\midrule
\endhead
\midrule
\multicolumn{5}{r}{\itshape continued on next page} \\
\endfoot
\bottomrule
\endlastfoot

R1 --- query-level inference & \blocked & 60\% (tool done, data absent) &
Same day the exports are mounted & \textbf{Meaning:} puts every $p$-value in
the paper on the unit the paper's own E1 analysis uses. Pure re-analysis of
existing per-query CSVs: run \texttt{analyze\_direction\_transfer.py --unit
query} and the equivalent for the EOT sweep over each export, then re-mark
Tables~1--3, Fig.~3 and the supplement. Needs the export trees; no GPU. This
is the only minimum-package item still open. \\

D6 --- held-out transforms for the hardened direction (R3) & \ready & 40\%
(code and tests done) & GPU-on $+$ 6\,h per backbone & \textbf{Meaning:} the
standard overfitting check for EOT, and the evidence the ``design variable''
claim currently lacks. Per backbone, two invocations of
\texttt{run\_direction\_transfer\_study.py --objective self --seeds 1234 1235
1236 --eval\_sanitizers none jpeg75 jpeg50 blur denoise jpeg60 jpeg30 median3
resize\_half blur2 bitdepth4 random\_one jpeg50\_blur}, one without and one
with \texttt{--eot\_sanitizers jpeg75 jpeg50 blur denoise}. One optimisation
pass per (query, condition, seed); the 13 transforms are forward passes only.
Commands in \texttt{docs/Design.md}. \\

D7 --- utility of the hardened direction (R4) & \ready & 40\% (code done)
& GPU-on $+$ 1\,h & \textbf{Meaning:} the paper recommends the hardened
variant and reports the unhardened variant's utility cost.
\texttt{evaluate\_direction\_utility.py --eot\_sanitizers jpeg75 jpeg50 blur
denoise}, same 200+200 manifests, three runs. Fits the local RTX 4050 if the
manifests and surrogate weights are present. \\

R8 decision --- supplement length & \pending & 0\% & On decision &
\textbf{Meaning:} desk-check risk at submission. See Table~1. \\

\end{longtable}

\section*{Table 3: Manuscript Edits}

\begin{longtable}{@{}p{0.05\linewidth}p{0.30\linewidth}p{0.10\linewidth}p{0.47\linewidth}@{}}
\caption{Text-only items from the fourth review, all closed on 8 September
2026 and kept visible for one cycle so the next review does not rediscover
them.}
\label{tab:tifs-text}\\
\toprule
ID & Edit & Status & Location \\
\midrule
\endfirsthead
\toprule
ID & Edit & Status & Location \\
\midrule
\endhead
\midrule
\multicolumn{4}{r}{\itshape continued on next page} \\
\endfoot
\bottomrule
\endlastfoot

T8 (R6) & Abstract 538 $\to$ 246 words (measured on the rendered page);
\texttt{IEEEkeywords} added. & \done & \texttt{main.tex} front matter. \\

T9 (R2) & Related-Work paragraph ``Adversarial perturbation as a privacy
mechanism''; 11 bibliography entries; citations at the point of use for the
sign-gradient perturbation, the surrogate ensemble, EOT, BPDA and the
adaptive attack; Contributions repositioned. & \done & \texttt{main.tex}
\S\,Related Work, \S\,The Other Axis, \S\,Robustness, \S\,Adaptive;
\texttt{ref.bib}. \\

T10 (R7) & ``How the direction is computed'' paragraph; the two white-box
numbers bridged; code URL restored in the author footnote. The EOT
samples-per-step count is deliberately not stated: the D5 launch command was
not recorded and the script default cannot be confirmed as what ran. & \done
& \texttt{main.tex} \S\,The Other Axis; title block. \\

T11 (R5) & Three figures: protocol schematic (TikZ), budget dependence,
preprocessing and hardening; the preprocessing table moved to the
supplement. Figure script: \texttt{src/scripts/make\_tifs\_review\_figures.py}. &
\done & \texttt{main.tex} \S\,Experimental Protocol, \S\,Null on Real Data,
\S\,Robustness; \texttt{supplementary.tex} new section; \texttt{paper/figs/}. \\

T12 (R9, R10) & Scope-of-claims rewritten as three enumerated claims; four
named passages and a further ten compressed to fund the figures; two
consistency fixes. Main text held at 13 pages. & \done & \texttt{main.tex}
throughout. \\

T13 & The standalone supplement cited MSLS without a reference list;
\texttt{\textbackslash bibliography} added. & \done & \texttt{supplementary.tex}. \\

\end{longtable}

\section*{Closed in Previous Cycles}

D1--D5 and every item of the third review (R1--R12 of 7 September) are
complete and recorded in \texttt{docs/progress.md} entries 226--262; the
appendix consolidation, the abstract number correction and the bibtex fault
are entries 255--262. ICME 2027 (M1--M9), TOMM (E1--E7) and the 3 September
items (F1, F2, F4--F12) remain closed.

\end{document}
