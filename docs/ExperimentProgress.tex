\documentclass[11pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{xcolor}
\usepackage[hidelinks]{hyperref}

\setlength{\emergencystretch}{3em}

\definecolor{DoneGreen}{RGB}{20,115,70}
\definecolor{RunOrange}{RGB}{180,90,0}
\definecolor{PendRed}{RGB}{160,25,25}
\newcommand{\done}{\textcolor{DoneGreen}{\textbf{$\checkmark$ Done}}}
\newcommand{\running}{\textcolor{RunOrange}{\textbf{Running}}}
\newcommand{\pending}{\textcolor{PendRed}{\textbf{Pending}}}
\newcommand{\deferred}{\textcolor{RunOrange}{Deferred}}
\newcommand{\blocked}{\textcolor{PendRed}{\textbf{Blocked}}}
\newcommand{\skipped}{Skipped (optional)}
\newcommand{\partialstatus}{\textcolor{RunOrange}{\textbf{Partial}}}

\title{\textbf{Experiment Progress and Next-Step Plan}}
\author{}
\date{8 September 2026 --- IEEE TIFS revision cycle. This file carries
\emph{only} open, running and not-yet-started work, per the repository
convention that completed items are removed from these tables once they are
recorded in \texttt{docs/progress.md}.}

\begin{document}
\maketitle

\section*{Scope of This Revision Cycle}

The third independent review in \texttt{docs/RevisionSuggestions.tex}
(7 September 2026, IEEE TIFS) is \textbf{exhausted}: every actionable item it
raised --- R1--R3, R5--R8, R10--R12 and the R4 page compression --- is closed,
and the five experiments it required (D1--D5) have landed in the manuscript.
What remains below is not review work. It is one page-budget decision, one
documentation backfill that is blocked on data this machine cannot reach, and
one disposition (B3) that is deliberately disclosed rather than closed.

Because no actionable review item remains, the repository rule that applies
next is the fresh independent review protocol: start a new venue-driven review
from the current \texttt{main.tex} and \texttt{supplementary.tex} and rewrite
\texttt{docs/RevisionSuggestions.tex} from scratch. That is the single
scheduled next action in Table~2, and it is held pending an explicit go-ahead.

\paragraph{Document state, rebuilt and measured on 8 September 2026.}
\texttt{main.pdf} 13 pages (the TIFS initial-submission hard limit),
\texttt{supplementary.pdf} 12 pages, both with 0 undefined references,
0 undefined citations and 0 overfull boxes. \texttt{appendix.tex} no longer
exists as a separate document: its unique evidence was merged into
\texttt{supplementary.tex} and the file was retired to
\texttt{paper/backup/appendix\_pre\_supplementary\_merge.tex}. The main text
contains no appendix sections.

\section*{Table 1: Open and Partial Items}

\begin{longtable}{@{}p{0.05\linewidth}p{0.27\linewidth}p{0.12\linewidth}p{0.48\linewidth}@{}}
\caption{Everything still open after the third TIFS review cycle. Completed
items are not listed; their records are in \texttt{docs/progress.md}.}
\label{tab:tifs-open}\\
\toprule
ID & Item & Status & Completion gate / next step \\
\midrule
\endfirsthead
\toprule
ID & Item & Status & Completion gate / next step \\
\midrule
\endhead
\midrule
\multicolumn{4}{r}{\itshape continued on next page} \\
\endfoot
\bottomrule
\endlastfoot

A1 & Appendix page budget. Merging the retired \texttt{appendix.tex} back into
\texttt{supplementary.tex} restored roughly 2.6 columns of evidence that the
earlier compression had dropped --- the full energy-matched control data
(pooled and per-backbone tables), the proxy50 scaling table and the
margin-subgroup decomposition --- which takes the appendix from 11 to 12
pages. Collapsing two subsections that restated other sections of the same
document returned about 0.8 columns, which was not enough to cross the page
boundary. & \pending & A decision, not a task. Either accept 12 pages (TIFS
recommends $\leq 6$ for supplementary material as \emph{guidance}, not a cap,
so 11 and 12 are equally non-compliant with the recommendation and equally
permitted), or free about 1.3 columns. The cheapest 1.3 columns are the
proxy50 fixed-budget table and its prose, which is the most legacy-framed of
the restored material. Nothing else can be cut without losing evidence. \\

F3 & Paper-facing CSV/JSON exports under the git-ignored
\texttt{src/outputs/} have SHA-256 provenance for the reachable export trees
only; the E1 MSLS and E5 KITTI-360 trees are still missing. &
\blocked & Blocked on data, not on effort: the
\texttt{src/outputs/icme2027\_*} export trees are not on this machine (the
drive holding the second copy is not mounted here), so neither the checksums nor the
\texttt{verify\_claims.py} artifact run can be reproduced locally. Backfill
into \texttt{docs/experiment\_provenance.md} when those trees are next
reachable. Non-blocking for submission. \\

B3 & Neither checkpoint of the tested mechanism was engineered as a privacy
map: the released one is numerically constant, the mask-supervised one was
trained for the attribution diagnostic. & \partialstatus & Disclosed rather
than closed, which is the correct disposition for a falsification testbed. No
further action planned; listed so it is not silently dropped. \\

V1 & The venue line in \texttt{USAGE.md} had reverted to ICLR 2027, undoing
the correction recorded as \texttt{progress.md} entry 227. Any automated cycle
reading that line would have reviewed an IEEEtran single-anonymised 13-page
TIFS submission against ICLR's double-blind 9-page rules. & \done &
Restored to IEEE TIFS on 8 September 2026, with the page, supplementary and
single-anonymised rules written back into that section and PoPETs kept as the
documented fallback. Listed here because the regression happened once and can
happen again whenever the rule template is re-copied into this repository. \\

\end{longtable}

\section*{Table 2: Next-Step Plan}

\begin{longtable}{@{}p{0.20\linewidth}p{0.08\linewidth}p{0.06\linewidth}p{0.16\linewidth}p{0.42\linewidth}@{}}
\caption{Scheduled work as of 8 September 2026. No experiment is running: the
D1--D5 programme is complete and both billed GPU hosts are confirmed powered
down. Estimated completion times are New Zealand Standard Time.}
\label{tab:tifs-schedule}\\
\toprule
Item & Status & \% & Est.\ completion (NZST) & Purpose and notes \\
\midrule
\endfirsthead
\toprule
Item & Status & \% & Est.\ completion (NZST) & Purpose and notes \\
\midrule
\endhead
\midrule
\multicolumn{5}{r}{\itshape continued on next page} \\
\endfoot
\bottomrule
\endlastfoot

Fresh independent review (Stage 9') & \pending & 0\% & On go-ahead;
same-day once started & \textbf{Meaning:} the current review file has no
actionable item left, so the repository rule is to review the manuscript again
from scratch against IEEE TIFS and overwrite
\texttt{docs/RevisionSuggestions.tex} in English-only \LaTeX. The manuscript
has changed substantially since the 7 September review --- gallery-free
objective promoted to the headline claim, the two preprocessing tables merged,
EOT hardening added, the body compressed from 15 to 13 pages and the appendix
merged --- so the previous review no longer describes the document under
review. Held pending an explicit go-ahead. No GPU required. \\

A1 decision --- appendix page budget & \pending & 0\% & On decision &
\textbf{Meaning:} whether the appendix ships at 12 pages or is cut back to 11.
See Table~1, row A1, for what the 1.3 columns would have to come out of. No
GPU required. \\

F3 checksum backfill & \blocked & --- & When the export trees are
reachable & \textbf{Meaning:} completes the provenance record for the E1 MSLS
and E5 KITTI-360 export trees. Cannot start on this machine; needs the
\texttt{src/outputs/icme2027\_*} trees mounted. This also gates any local
re-run of \texttt{src/artifact/verify\_claims.py}, whose last recorded run
(228 checks, 0 mismatches) was made where those exports were reachable. \\

\end{longtable}

\section*{Table 3: Manuscript Edits}

\begin{longtable}{@{}p{0.05\linewidth}p{0.30\linewidth}p{0.10\linewidth}p{0.47\linewidth}@{}}
\caption{Text-only revision items. All items from the third TIFS review
(T1--T4) are closed and removed; the rows below are the edits made on
8 September 2026 that are not yet covered by any review file, kept visible for
one cycle so the next review does not rediscover them as findings.}
\label{tab:tifs-text}\\
\toprule
ID & Edit & Status & Location \\
\midrule
\endfirsthead
\toprule
ID & Edit & Status & Location \\
\midrule
\endhead
\midrule
\multicolumn{4}{r}{\itshape continued on next page} \\
\endfoot
\bottomrule
\endlastfoot

T5 & The abstract still reported the preprocessing white-box recovery in the
superseded reference-targeted terms ($0.05 \to 0.47$--$0.59$) while the body
reported the gallery-free measurement of the same quantity
($0.0008 \to 0.5625$--$0.7042$), so the abstract contradicted its own results
section. Rewritten to the gallery-free range and extended to state the two
non-significant cells and their repair by EOT. & \done &
\texttt{main.tex} abstract. The 7 September audit missed it because it scanned
for the full-precision string \texttt{0.4717}; the abstract carried the
rounded form. \\

T6 & Merge the retired standalone appendix into the single appendix document
and remove the duplication the earlier compression left behind. & \done &
\texttt{supplementary.tex}: energy-matched control data restored, proxy50
scaling and margin decomposition added, the two matched-PSNR subsections moved
out of the sequence-length section they had been filed under, and the two
secondary-benchmark subsections that restated other sections of the same file
collapsed to pointers. \\

T7 & Promote the margin decomposition into the main text: an additive budget
moves the queries whose margin was already small ($0.465 \to 0.377$) and
leaves confident ones alone ($1.000 \to 0.987$), which is the empirical form
of the mechanism section's own argument and explains the spread in aggregate
deltas across attacker backbones. & \done & \texttt{main.tex}
\S\,Why Allocation Cannot Decide a Ranking. Main text stays at 13 pages. \\

\end{longtable}

\section*{Closed in This Cycle}

D1 (gallery-free direction objective), D2 (adaptive adversary trained on the
defended output), D3 (downstream utility of the direction perturbation),
D4 (three-seed preprocessing sweep) and D5 (EOT hardening) are complete on
both backbones, written into the manuscript, and recorded with their evidence
in \texttt{docs/progress.md} entries 246--255. The R4 compression to 13 pages
was performed last, as scheduled, and verified against the rendered PDF rather
than the source. The ICME 2027 items (M1--M9), the TOMM cycle (E1--E7) and the
3 September fresh-review items (F1, F2, F4--F12) remain closed from earlier
cycles.

\end{document}
